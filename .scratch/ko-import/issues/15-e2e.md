# 15 · e2e suite

Type: task
Status: resolved
Repo: lineup-manager
Blocked by: 14
Branch: `feat/15-e2e` (commit b5acdff, stacked on feat/14-bracket-view-ux)

## Scope

Spec §8: isolated, re-runnable-without-reset fixtures with a full bracket (pool, empty slots, later rounds); flows — import v2, entry, wrong-event/duplicate-slot blocks, removal with placement release, placement, balance + advance-byes, winner propagation to the Final, un-pick instant and cascade (incl. submitted-lineup stand vs remove), v1 rejection. RLS specs: null-side invisibility, one-side-known visibility on landing. REST-level assertions use specific error codes per house convention.

## Answer

Done 2026-08-19. Three new specs + fixtures on isolated, re-runnable categories (uniform-key upserts in global-setup; the lifecycle spec resets its rows in beforeAll):

- **`ko-bracket.spec.ts`** (UI): the full worked example through the bracket view — entry with deliberate byes (QF1A/QF4A), duplicate-slot and wrong-category picker guardrails, placement of both pool matches, advance-on-balance, winners propagating to the Final, instant un-pick on the Final, the cascade confirmation rewinding the semis and Final, and ✕ removal releasing the placement (chip unbinds).
- **`ko-rls.spec.ts`** (REST, error code 42501): null-side KO rows invisible to the category's manager until the admin lands their team (then visible immediately); the tightened lineup-membership policy — a phantom write fails 42501, the same write succeeds once the team occupies a side (unscheduled slots never lock); rows converge for the re-run.
- **`import.spec.ts`**: moved to v2 with the parser and gained a KO-seed import test (structure renders in the bracket view) plus the v1 rejection at the version gate.

**Three real bugs found and fixed by the suite:** (1) the tournament store crashed on NULL `scheduled_start` in its last-tie embed (Postgres orders NULLs first under DESC → `.slice` on null) — null-safed; (2) no deterministic within-round slot order — imported slots mint random ids, so the DB's unordered select crossed the feeds — new `bracket_slot` column (migration 0014), written at import (fed ties parse their positional seed id), ordered in the snapshot; (3) rapid bracket clicks were silently swallowed by the service busy-guard — interactive controls now disable while busy. Also: pool labels render tournament-local (trimmed), PostgREST RLS violations assert 403/400 + code 42501. Full e2e suite **27 passed**; unit suite 235 green; vue-tsc clean.

All seven ko-import build tickets (09–15) are now closed across both repos.
